<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <!-- Modul für allgemeine Formatierung -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- Modul zur Überprüfung der Zeilenlänge -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <!-- Paket- und Import-Zeilen sollen ignoriert werden -->
        <property name="ignorePattern" value="^package.*|^import.*"/>
    </module>

    <!-- Modul, das den Code-Strukturbaum analysiert -->
    <module name="TreeWalker">
        <!-- Namenskonvention für Klassen -->
        <module name="TypeName">
            <property name="format" value="^[A-Z][a-zA-Z0-9]*$"/>
        </module>

        <!-- Überprüfung der Methoden-Dokumentation (nur grundlegende Einstellungen) -->
        <module name="JavadocMethod">
            <property name="allowMissingParamTags" value="true"/>
            <!-- Eigenschaft allowMissingThrowsTags wurde entfernt -->
        </module>

        <!-- Überprüfung der Methodenlänge -->
        <module name="MethodLength">
            <property name="max" value="50"/>
        </module>

        <!-- Überprüfung auf überflüssige Inline-Bedingungen -->
        <module name="AvoidInlineConditionals"/>

        <!-- Überprüfung auf leere Blöcke -->
        <module name="EmptyBlock">
            <property name="option" value="TEXT"/>
        </module>

        <!-- Allgemeine Überprüfung der Leerzeichen -->
        <module name="WhitespaceAround"/>
    </module>
</module>
